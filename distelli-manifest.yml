user1/Example-Node:
  # Distelli Manifest example
  # this example assumes the build and deploy servers are:
  # Ubuntu 14.04 

  Build:
  - echo "build"

  PkgInclude:
    - '*'

  PreInstall:
    - echo "pre install"

  Env:
    - PORT: 3000
